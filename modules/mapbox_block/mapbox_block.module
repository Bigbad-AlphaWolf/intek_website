<?php

/**
 * @file
 * Contains mapbox_block.module.
 */

/**
 * Implements hook_theme().
 */
function mapbox_block_theme($existing, $type, $theme, $path) {
  return [
    'mapbox' => [
      'variables' => [
        'map_id' => NULL,
      ],
      'file' => 'mapbox_block.preprocess.inc',
    ],
  ];
}
